[package]
name = "rust-container"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.180"
nix = { version = "0.27.1", features = ["ptrace", "user", "process"] }
rand = "0.9.2"
syscalls = "0.6"

[dev-dependencies]

[workspace]
members = [
    ".",
]
default-members = [
    ".",
]
